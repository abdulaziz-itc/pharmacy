import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('PharmaRep app smoke test', (WidgetTester tester) async {
    // App requires ProviderScope and secure storage initialization
    // Integration tests should be run separately
    expect(true, isTrue);
  });
}
